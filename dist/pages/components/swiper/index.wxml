
  <view class="page-swiper__container">
    
  <view class="com-container__container">
    <view class="example example--wrap">
      <view  wx:if="{{$comContainer$title || $comContainer$desc}}" class="example__head">
          <view wx:if="{{$comContainer$title}}" class="example__head--title">{{$comContainer$title}}</view>
          <view wx:if="{{$comContainer$title && $comContainer$desc}}" class="example__head--line"/>
          <view wx:if="{{$comContainer$desc}}" class="example__head--desc">{{$comContainer$desc}}</view>
      </view>
      
      
  <view class="com-card__container">
    <view class="example--box">
      <view class="example--demo demo">
        <view class="demo-inner">
          
        
  <view class="com-swiper__container">
    <swiper class="kk-swiper" indicator-dots="{{$swiper$indicatorDots}}" autoplay="{{$swiper$autoplay}}" interval="{{$swiper$interval}}" duration="{{$swiper$duration}}" current="{{$swiper$current}}" circular="{{$swiper$iscircular}}" bindchange="$swiper$handleChange">
			<block wx:for="{{$swiper$imgs}}" wx:for-index="$swiper$index" wx:for-item="$swiper$item" wx:key="$swiper$key">
        <swiper-item>
          <image src="{{$swiper$item}}" class="kk-swiper__image" mode="widthFix"/>
        </swiper-item>
      </block>
    </swiper>
  </view>

      
        </view>
      </view>
      <view class="example--meta meta">
        <view class="meta-title">{{$comCard$title}}</view>
        <view class="meta-desc">{{$comCard$desc}}</view>
      </view>
      <!-- <view class="example--source">
          <view class="view-btn {{viewSourceClass}}" bindtap="bindViewSourceEvent">{{ isExpandSource ? 'Hide' : 'Show' }} Code</view>
          <example-md wx:if="{{isExpandSource}}" content="{{source}}" type="demo" _pid="{{_pid}}"></example-md>
      </view> -->
    </view>
  </view>

      
  <view class="com-card__container">
    <view class="example--box">
      <view class="example--demo demo">
        <view class="demo-inner">
          
        
  <view class="com-verticalSwiper__container">
    <swiper class="kk-verticalSwiper" indicator-dots="{{$swiperVertical$indicatorDots}}" autoplay="{{$swiperVertical$autoplay}}" interval="{{$swiperVertical$interval}}" duration="{{$swiperVertical$duration}}" current="{{$swiperVertical$current}}" circular="{{$swiperVertical$iscircular}}" vertical="{{$swiperVertical$vertical}}">
			<block wx:for="{{$swiperVertical$imgs}}" wx:for-index="$swiperVertical$index" wx:for-item="$swiperVertical$item" wx:key="$swiperVertical$key">
        <swiper-item>
          <view class="kk-verticalSwiper__box">
            <view class="kk-verticalSwiper__icon">
              <image src="{{$swiperVertical$item.icon}}"/>
            </view>
            <view class="kk-verticalSwiper__text">{{$swiperVertical$item.text}}</view>
          </view>
        </swiper-item>
      </block>
    </swiper>
  </view>

      
        </view>
      </view>
      <view class="example--meta meta">
        <view class="meta-title">{{$comCard$title}}</view>
        <view class="meta-desc">{{$comCard$desc}}</view>
      </view>
      <!-- <view class="example--source">
          <view class="view-btn {{viewSourceClass}}" bindtap="bindViewSourceEvent">{{ isExpandSource ? 'Hide' : 'Show' }} Code</view>
          <example-md wx:if="{{isExpandSource}}" content="{{source}}" type="demo" _pid="{{_pid}}"></example-md>
      </view> -->
    </view>
  </view>

    
    </view>
  </view>

  </view>